{
  "name": "syntaxis-compliance-checker",
  "displayName": "Syntaxis Compliance Checker",
  "version": "0.8.0",
  "description": "Revisa código contra Leyes 21.719 y 21.663 en tiempo real — Análisis Estático + Análisis con IA",
  "author": "Syntaxis SPA",
  "license": "MIT",
  "publisher": "syntaxis",
  "repository": {
    "type": "git",
    "url": "https://github.com/rchamycruz/compliance-checker"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Linters",
    "Other"
  ],
  "main": "./out/extension.js",
  "icon": "icon.png",
  "activationEvents": [
    "onStartupFinished",
    "onLanguage:csharp",
    "onLanguage:javascript",
    "onLanguage:typescript",
    "onCommand:syntaxis.checkFile",
    "onCommand:syntaxis.checkWorkspace",
    "onCommand:syntaxis.generateReport"
  ],
  "contributes": {
    "commands": [
      {
        "command": "syntaxis.checkFile",
        "title": "Revisar archivo actual",
        "category": "Syntaxis"
      },
      {
        "command": "syntaxis.checkWorkspace",
        "title": "Revisar workspace completo",
        "category": "Syntaxis"
      },
      {
        "command": "syntaxis.generateReport",
        "title": "Generar reporte",
        "category": "Syntaxis"
      },
      {
        "command": "syntaxis.configureApiKey",
        "title": "Configurar API Key de IA",
        "category": "Syntaxis"
      },
      {
        "command": "syntaxis.testAiConnection",
        "title": "Verificar conexión con IA",
        "category": "Syntaxis"
      }
    ],
    "configuration": {
      "title": "Syntaxis Compliance Checker",
      "properties": {
        "syntaxis.analysisMode": {
          "type": "string",
          "enum": ["static", "ai"],
          "enumDescriptions": [
            "Análisis Estático: reglas deterministas, rápido, sin conexión a internet",
            "Análisis con IA: agentes LLM especializados por ley, mayor profundidad semántica"
          ],
          "default": "static",
          "description": "Modo de análisis de compliance. 'static' usa reglas regex offline; 'ai' usa modelos de lenguaje especializados por cada ley."
        },
        "syntaxis.ai.provider": {
          "type": "string",
          "enum": ["github-copilot", "openai", "anthropic", "azure-openai"],
          "enumDescriptions": [
            "GitHub Copilot — usa tu suscripción activa de Copilot (sin API key adicional)",
            "OpenAI — GPT-4o, GPT-4o-mini, o1-mini (requiere API key)",
            "Anthropic — Claude 3.5 Sonnet, Claude 3 Haiku (requiere API key)",
            "Azure OpenAI — endpoint propio con modelo desplegado (requiere API key y endpoint)"
          ],
          "default": "github-copilot",
          "description": "Proveedor de IA para el análisis. GitHub Copilot no requiere API key adicional."
        },
        "syntaxis.ai.model": {
          "type": "string",
          "default": "gpt-4o-mini",
          "description": "Modelo a usar para el análisis. Ejemplos: gpt-4o, gpt-4o-mini, claude-3-5-haiku-latest. Ignorado para GitHub Copilot (se selecciona automáticamente)."
        },
        "syntaxis.ai.azureEndpoint": {
          "type": "string",
          "default": "",
          "description": "URL base del endpoint de Azure OpenAI. Ej: https://mi-recurso.openai.azure.com. Solo requerido para provider azure-openai."
        }
      }
    }
  },
  "scripts": {
    "compile": "node esbuild.js",
    "compile:tsc": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "node esbuild.js && vsce package",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.60.0",
    "@types/node": "^20.0.0",
    "@types/mocha": "^10.0.0",
    "typescript": "^5.0.0",
    "esbuild": "^0.20.0"
  }
}
